#include<iostream>
#include<string> 
#include<vector>

using namespace std ; 

bool isPalindrome(int first, int  last, string sentence);

int main()
{
  string sentence = "Able was I, ere I saw Elba" ;
  string sentence2 = "A man, a plan, a canal, Panama" ; 
  string check = sentence ; 
 
  if(sentence== string(sentence.rbegin(),sentence.rend()))
    cout << "yes" << endl ; 

  return 0 ; 
}
